require 'rails_helper'

RSpec.describe 'google_geocoding', type: :feature do
  # service = GoogleService.new('denver,co')
  #
  # # binding.pry
  # expect(service.city).to eq('denver')
  # expect(service.state).to eq('colorado')
end
